# Nightly QA Log

- Lint: `npm run lint` (pass)
- Tests: `npm run test` (Vitest) — scoring, enrichment, jobber mapping all passing (7/7)
- Known gaps: integration tests for Jobber API routes and Supabase persistence not executed (env-bound).

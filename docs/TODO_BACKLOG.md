# TODO Backlog (post-v1)
- Wire admin authentication to an edge middleware or identity provider; current shared-secret guard is optional.
- Add persistent telemetry store (provider uptime/errors) beyond in-memory.
- Expand provider integrations beyond mock/heuristic (requires API keys).
- Add automated DB migration tooling and rollback script.
- Add E2E browser tests for admin workflows and property detail navigation.
